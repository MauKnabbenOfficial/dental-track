import {
  Treatment,
  // Archived implementation moved to `src/services/archive/mock/MockTreatmentService.ts`.
  // Runtime mocks are disabled. Keep this as a harmless stub to avoid
  // accidental runtime usage.
  export {};
import {
